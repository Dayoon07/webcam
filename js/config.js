const Config = {
    // 필터 옵션 설정
    // 추가하고 싶은게 있으면
    /*
        필드명: '명칭'
    */
    FILTER_OPTIONS: {
        COWBOY: 'cowboy',
        EYE_HEART: 'eyeHeart'
    },

    // 얼굴 인식 설정
    DETECTION_OPTIONS: {
        withLandmarks: true,
        withDescriptors: false
    }
};